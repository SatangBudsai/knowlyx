# sage-review

**Review a change for correctness and conformance before it ships**

**Use when:** the user runs `/sage-review`, or implementation finished and the change needs
a correctness and requirement-conformance review.

Thin adapter — the command body lives in **one** place:
**`agents/sage/commands/sage-review.md`**. Read that file and follow it verbatim.
Do not duplicate its steps here; edit the canonical file to change behaviour.
