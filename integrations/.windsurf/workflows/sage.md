# Sage

1. Read `agents/sage/AGENTS.md`.
2. Read and follow `agents/sage/commands/sage.md` as the canonical Sage command.
3. Do not duplicate or bypass the protocol; continue only when its gates allow.
