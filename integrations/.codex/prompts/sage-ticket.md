# sage-ticket

**Cut clear requirements into implementation tickets, then build them**

**Use when:** the user runs `/sage-ticket`, or a `requirements-clear` / `design-clear`
handoff is ready to be sliced into build work.

Thin adapter — the command body lives in **one** place:
**`agents/sage/commands/sage-ticket.md`**. Read that file and follow it verbatim.
Do not duplicate its steps here; edit the canonical file to change behaviour.
