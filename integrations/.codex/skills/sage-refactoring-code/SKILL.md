---
name: sage-refactoring-code
description: Write or refactor application code, components, functions, utilities, modules, and database schemas for human readability and practical adaptability. Use when code is too nested, abstract, fragmented, vaguely named, difficult to move or change, over-engineered for hypothetical cases, or inconsistent with clear project conventions; also use when implementing new code that should stay simple without sacrificing correctness, security, data integrity, or public behavior.
---

# Sage Refactoring Code

Read and follow `agents/sage/AGENTS.md`, then execute the canonical command in
`agents/sage/commands/sage-refactoring-code.md`.

Keep this file as a thin discovery adapter. The canonical command owns the full
workflow so Codex and every other Sage integration use the same rules.
