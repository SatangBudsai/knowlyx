---
name: sage
description: Run the Sage cognition protocol before a code-changing task.
---

# Sage

Use this skill when the user invokes Sage or asks to run the Sage protocol.

Read and follow `agents/sage/commands/sage.md` as the canonical command body.
The protocol source of truth is `agents/sage/AGENTS.md`. Do not duplicate or
rewrite the pipeline here.

Codex surfaces may expose this as `$sage` or `/prompts:sage`; the exact command
label is owned by Codex, not by this repository.
